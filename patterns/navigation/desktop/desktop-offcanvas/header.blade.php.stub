@php($items = config('laravel-site.navigation.header', []))
<header class="nav-offcanvas-bar"><a href="/"><x-site.logo /></a><button type="button" data-nav-open aria-controls="desktop-menu" aria-expanded="false">메뉴</button></header>
<div id="desktop-menu" class="nav-offcanvas" data-nav-panel aria-hidden="true" hidden>
    <button type="button" data-nav-close aria-label="메뉴 닫기">닫기</button>
    <nav aria-label="주요 메뉴">@foreach ($items as $item)<a href="{{ $item['url'] ?? '#' }}">{{ $item['label'] ?? '메뉴' }}</a>@endforeach</nav>
</div>
