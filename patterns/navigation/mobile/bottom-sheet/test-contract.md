# Test contract

- 열기 버튼은 `aria-controls="site-mobile-menu"`와 `aria-expanded`를 가진다.
- Escape, backdrop, 내부 링크로 닫히고 열기 버튼으로 focus가 복귀한다.
- modal 메뉴는 focus trap과 body scroll lock을 제공한다.
- 320px에서 가로 스크롤과 가려진 닫기 버튼이 없다.
- reduced motion에서 전환이 제거된다.
