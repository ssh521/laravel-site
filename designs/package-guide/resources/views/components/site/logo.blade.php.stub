@props([
    'href' => '/',
    'label' => config('laravel-site.brand.logo_text', config('laravel-site.brand.name')),
])

<a href="{{ $href }}" {{ $attributes->merge(['class' => 'inline-flex items-center text-(--site-color-text)']) }}>
    <span class="text-lg font-black uppercase leading-none">
        {{ $label }}<span class="text-(--site-color-accent)">.</span>
    </span>
</a>
